spring:
  profiles:
    active: verb
  jackson:
    serialization:
      indent-output: true
  application:
    name: verb
  config:
    import: optional:application.yaml
  cloud:
    config:
      uri: http://localhost:8888/config
