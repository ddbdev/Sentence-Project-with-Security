spring:
  profiles:
    active: object
  jackson:
    serialization:
      indent-output: true
  application:
    name: object
  config:
    import: optional:application.yaml
  cloud:
    config:
      uri: http://localhost:8888/config
