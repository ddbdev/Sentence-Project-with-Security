spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ddbdev/Sentence-Project-with-Security
          username: ${GITUSER}
          password: ${AUTHTOKEN}
        default-label: main
        enabled: true